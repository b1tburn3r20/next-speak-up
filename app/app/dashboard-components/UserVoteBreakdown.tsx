// has to be a server component to get data. Then if needed pass data to
// client with react suspense to render the fallback.
const UserVoteBreakdown = () => {
  return <div>UserVoteBreakdown</div>;
};

export default UserVoteBreakdown;
